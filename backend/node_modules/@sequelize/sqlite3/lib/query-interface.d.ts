import type { AddConstraintOptions, AttributeOptions, ConstraintDescription, DataType, DescribeTableOptions, QiDropAllTablesOptions, QueryRawOptions, RemoveColumnOptions, RemoveConstraintOptions, ShowConstraintsOptions, TableOrModel } from '@sequelize/core';
import { AbstractQueryInterface } from '@sequelize/core';
import type { SqliteDialect } from './dialect.js';
import { SqliteQueryInterfaceInternal } from './query-interface.internal.js';
import type { SqliteColumnsDescription } from './query-interface.types.js';
export declare class SqliteQueryInterface<Dialect extends SqliteDialect = SqliteDialect> extends AbstractQueryInterface<Dialect> {
    #private;
    constructor(dialect: Dialect, internalQueryInterface?: SqliteQueryInterfaceInternal);
    dropAllTables(options?: QiDropAllTablesOptions): Promise<void>;
    describeTable(tableName: TableOrModel, options?: DescribeTableOptions): Promise<SqliteColumnsDescription>;
    addConstraint(tableName: TableOrModel, options: AddConstraintOptions): Promise<void>;
    removeConstraint(tableName: TableOrModel, constraintName: string, options?: RemoveConstraintOptions): Promise<void>;
    showConstraints(tableName: TableOrModel, options?: ShowConstraintsOptions): Promise<ConstraintDescription[]>;
    /**
     * A wrapper that fixes SQLite's inability to remove columns from existing tables.
     * It will create a backup of the table, drop the table afterwards and create a
     * new table with the same name but without the obsolete column.
     *
     * @param tableName
     * @param removeColumn
     * @param options
     */
    removeColumn(tableName: TableOrModel, removeColumn: string, options?: RemoveColumnOptions): Promise<void>;
    /**
     * A wrapper that fixes SQLite's inability to change columns from existing tables.
     * It will create a backup of the table, drop the table afterwards and create a
     * new table with the same name but with a modified version of the respective column.
     *
     * @param tableName
     * @param columnName
     * @param dataTypeOrOptions
     * @param options
     */
    changeColumn(tableName: TableOrModel, columnName: string, dataTypeOrOptions: DataType | AttributeOptions, options?: QueryRawOptions): Promise<void>;
    /**
     * A wrapper that fixes SQLite's inability to rename columns from existing tables.
     * It will create a backup of the table, drop the table afterwards and create a
     * new table with the same name but with a renamed version of the respective column.
     *
     * @param tableName
     * @param attrNameBefore
     * @param attrNameAfter
     * @param options
     */
    renameColumn(tableName: TableOrModel, attrNameBefore: string, attrNameAfter: string, options?: QueryRawOptions): Promise<void>;
}
