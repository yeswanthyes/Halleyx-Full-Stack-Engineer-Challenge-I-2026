import type { QueryRawOptions, TableOrModel } from '@sequelize/core';
import { AbstractQueryInterfaceInternal } from '@sequelize/core/_non-semver-use-at-your-own-risk_/abstract-dialect/query-interface-internal.js';
import type { SqliteDialect } from './dialect.js';
import type { SqliteColumnsDescription } from './query-interface.types.js';
export declare class SqliteQueryInterfaceInternal extends AbstractQueryInterfaceInternal {
    #private;
    readonly dialect: SqliteDialect;
    constructor(dialect: SqliteDialect);
    /**
     * Alters a table in sqlite.
     * Workaround for sqlite's limited alter table support.
     *
     * @param tableName
     * @param columns
     * @param options
     */
    alterTableInternal(tableName: TableOrModel, columns: SqliteColumnsDescription, options?: QueryRawOptions): Promise<void>;
}
